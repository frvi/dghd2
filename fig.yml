influxdb:
  image: tutum/influxdb
  ports:
    - "8083"
    - "8086:8086"
  environment:
    PRE_CREATE_DB: metrics;grafana
grafanafiles:
  build: grafanafiles
grafana:
  build: grafana 
  links:
    - influxdb
  volumes_from:
    - grafanafiles
  ports:
    - "80" 
