influxdb:
  image: tutum/influxdb
  ports:
    - "8083"
    - "8086:8086"
  environment:
    PRE_CREATE_DB: metrics;grafana
grafana:
  build: grafana
nginx:
  build: nginx
  links:
    - influxdb
  volumes_from:
    - grafana
  ports:
    - "80"
    - "8086"
